spring.application.name=proyectoingweb

# Configuración de PostgreSQL para Render (con SSL)
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://dpg-d5hhtb24d50c73938ce0-a:5432/ingenieriaweb_oyt2?sslmode=require}
spring.datasource.username=${DB_USERNAME:ingenieriaweb_oyt2_user}
spring.datasource.password=${DB_PASSWORD:mKOXORjEgnMyMtz2gtDxi0OmRcNBaJd2}
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuraciones de JPA/Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# Configuraciones adicionales para PostgreSQL en Render
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.connection-timeout=30000

# Puerto dinámico para Railway/Render
server.port=${PORT:8080}

# CORS para producción (Render)
cors.allowed-origins=${FRONTEND_URL:https://sistema-vocacional-frontend.onrender.com}